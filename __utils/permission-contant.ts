export enum VIEW_PERMISSION {
    COHORT_SUMMARY = 'campaign.cohort.summary-section',
    CREATE_COHORT = 'campaign.cohort.create-cohort'
}

export enum ACTION_PERMISSION {
    CREATE_COHORT = 'campaign.create.create-cohort',
    COHORT_SUMMARY = 'campaign.cohort.summary'
}