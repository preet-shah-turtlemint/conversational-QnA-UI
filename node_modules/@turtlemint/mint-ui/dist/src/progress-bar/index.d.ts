import * as React from "react";
export interface ProgressBarProps {
    progress: number;
    title?: string;
    showProgressPercent?: boolean;
    customProgressBlock?: React.ReactElement;
}
export declare const ProgressBar: ({ progress, title, showProgressPercent, customProgressBlock }: ProgressBarProps) => JSX.Element;
export default ProgressBar;
