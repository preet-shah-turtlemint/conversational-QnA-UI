import * as React from "react";
interface ChipProps {
    text: string;
    /** If true shows a tick*/
    done?: boolean;
    style?: React.CSSProperties;
}
export declare const Chip: ({ text, done, style }: ChipProps) => JSX.Element;
export default Chip;
