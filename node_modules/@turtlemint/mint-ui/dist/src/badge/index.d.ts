import * as React from "react";
interface BadgeProps {
    text: string;
    style?: React.CSSProperties;
    adjustPadding?: boolean;
    badgeType?: "new" | "info" | "alert";
    color?: string;
    size?: number;
}
export declare const Counter: ({ text, style, color, size }: BadgeProps) => JSX.Element;
export declare const Badge: ({ text, style, color, size, badgeType }: BadgeProps) => JSX.Element;
export default Badge;
