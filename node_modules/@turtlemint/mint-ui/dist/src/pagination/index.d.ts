import * as React from "react";
export declare type PaginationCallbackProps = {
    currentPage: number;
    pageSize: number;
};
interface PaginationProps {
    total: number;
    current?: number;
    onChange?: ({ currentPage, pageSize }: PaginationCallbackProps) => void;
    defaultCurrent?: number;
    disabled?: boolean;
    pageSize?: number;
    style?: React.CSSProperties;
}
export declare const Pagination: ({ total, defaultCurrent, disabled, current, onChange, pageSize, style }: PaginationProps) => JSX.Element;
export default Pagination;
