import * as React from "react";
import { FormItemProps } from "./form-item";
import { FormRowProps } from "./row";
declare const displayTuple: ["horizontal", "vertical", "inline"];
export declare type DisplayType = typeof displayTuple[number];
interface FormProps {
    /** name of the form */
    name: string;
    /** onSubmit event handler of the form */
    onSubmit: (state: any, e?: React.FormEvent) => void;
    /** layout can take values from  horizontal, vertical and inline. Horizontal is default  */
    layout?: DisplayType;
    formState?: any;
    children: any;
}
interface CompoundedComponent extends React.ForwardRefExoticComponent<FormProps & React.RefAttributes<HTMLFormElement>> {
    Item: React.FC<FormItemProps>;
    Row: React.FC<FormRowProps>;
}
export declare const Form: CompoundedComponent;
export default Form;
