import * as React from "react";
export declare const ListContext: React.Context<any>;
declare const SizeValues: ["small", "default", "large"];
export declare type SizeType = typeof SizeValues[number];
export interface ListProps {
    size?: SizeType;
    bordered?: boolean;
    loading?: boolean;
    dataSource: any[];
    renderItem?: (item: any, index: any) => any;
}
export declare const List: {
    ({ size, bordered, loading, dataSource, renderItem }: ListProps): JSX.Element;
    Item: ({ avatar, title, text, actions }: import("./list-item").ListItemProps) => JSX.Element;
};
export default List;
