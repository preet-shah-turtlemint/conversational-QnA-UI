import { ColumnType, sortOrderType, ActiveSort } from "./index";
interface TheadProps {
    columns: ColumnType[];
    activeSort: ActiveSort;
    handleSort: (column: ColumnType, sortOrder: sortOrderType | undefined) => void;
}
declare const Thead: ({ columns, activeSort, handleSort }: TheadProps) => JSX.Element;
export default Thead;
