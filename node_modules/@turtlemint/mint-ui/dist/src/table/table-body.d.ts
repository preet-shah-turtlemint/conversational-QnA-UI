import { OnRowReturn } from "./index";
interface TableBodyProps {
    data: any;
    onRow?: (record: any, rowIndex: string) => OnRowReturn;
    pageSize: number;
    currentPage: number;
    isAjax: boolean;
}
declare const TableBody: ({ data, onRow, pageSize, currentPage, isAjax }: TableBodyProps) => JSX.Element;
export default TableBody;
